---
title: 我的第二篇文章
date: 2026-03-04 13:58:05
tags:
  - Openclaw
  - AI
  - 自动化
categories: 技术实践
cover:
---
Openclaw

openclaw-cn onboard

# 设置模型，API，厂商，名称。
customer可以添加任意厂商（白山，心流，Nvidia）

skill还不会

# 配置孵化，openclaw tui，
对话切换模型 /model XXXXX（名字一定要一样，大小写也要对）

# 配置QQ git clone https://github.com/sliverp/qqbot.git && cd qqbot
cd进文件夹 openclaw plugins install . （注意有个.）

# 重启网关

网关gateway 关不掉的话，ps aux | grep 18789（一定是这个）
kill -9 进程ID

# 前台启动
重启 openclaw gateway run

# 在后台运行
nohup openclaw-cn gateway run --allow-unconfigured > /tmp/gateway.log 2>&1 &

# 查看是否启动成功
sleep 2
netstat -tlnp | grep 18789

# 模型工作可能会延时，runing的话肯定在调用，其他的不一定，灰色的conenct肯定是待机状态，有的模型切换了之后要重启网关，否则不工作，调动不到的模型会调用”fallback“的模型，嗷嗷费钱，1个小时跑掉一杯COCO

# 暂时没其他的了。


# 还有DingTalk，传统通道，按照配置处理就行

先这些

目前在让openclaw帮我看邮件



